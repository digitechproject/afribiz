version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: afribiz_postgres
    restart: always
    environment:
      POSTGRES_USER: afribiz_user
      POSTGRES_PASSWORD: afribiz_pass_2026
      POSTGRES_DB: afribiz_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
